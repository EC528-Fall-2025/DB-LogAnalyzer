# FDB Log Analyzer Dependencies
# Python 3.10+ (required for importlib.metadata.packages_distributions)

# 核心依赖
duckdb>=0.9.0
pydantic>=2.0.0

# 可选依赖（用于数据导出）
pandas>=2.0.0
pyarrow>=14.0.0  # 用于parquet格式导出

# AI Agent依赖 (核心组件)
google-generativeai>=0.3.0  # Google Gemini API - AI Agent uses Gemini 2.5 Flash for analysis
google-api-core>=2.14.0  # Core Google API client library (fixes FutureWarning and importlib compatibility)
google-cloud-aiplatform>=1.70.0  # Vertex AI (used for regional RAG retrieval)
